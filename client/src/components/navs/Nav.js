import { IconButton } from '@material-ui/core';
import { Menu } from '@material-ui/icons';
import React from 'react';

const Nav = () => {
  return (
    <div>
      <IconButton>
       <Menu />
      </IconButton>
      <div>
        
      </div>
    </div>
  )
}

export default Nav
